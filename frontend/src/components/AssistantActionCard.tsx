import type { AssistantActionPreview, AssistantActionResult } from "../types";
export default function AssistantActionCard({action,onConfirm,onCancel,result,busy=false}:{action:AssistantActionPreview;onConfirm:()=>void;onCancel:()=>void;result?:AssistantActionResult|null;busy?:boolean}){
 const needs=action.confirmation_step<action.confirmation_steps_required;
 return <section className="rounded-xl border border-amber-200 bg-amber-50 p-4"><div className="flex items-center justify-between"><div><p className="font-semibold text-amber-900">{action.summary}</p><p className="mt-1 text-xs text-amber-700">{action.tool_name} · 风险：{action.risk_level} · 确认 {action.confirmation_step}/{action.confirmation_steps_required}</p></div><span className="rounded-full bg-white px-2 py-1 text-xs text-amber-700">{busy ? "处理中" : result?.status ?? "待确认"}</span></div>{needs&&<div className="mt-3 flex gap-2"><button className="rounded-md bg-amber-600 px-3 py-2 text-sm text-white disabled:cursor-not-allowed disabled:opacity-60" onClick={onConfirm} disabled={busy}>{busy ? "处理中..." : action.confirmation_phrase||"确认执行"}</button><button className="rounded-md border border-amber-300 px-3 py-2 text-sm text-amber-800 disabled:cursor-not-allowed disabled:opacity-60" onClick={onCancel} disabled={busy}>取消</button></div>}{result?.error_code&&<p className="mt-2 text-sm text-red-700">操作未执行：{result.error_code}</p>}</section>
}
